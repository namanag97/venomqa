base_url: "http://localhost:8000"
timeout: 30

retry:
  max_attempts: 3
  delay: 1
  backoff_multiplier: 2

report:
  formats:
    - html
    - json
    - junit
  output_dir: "./reports"

parallel_paths: 4
verbose: true
